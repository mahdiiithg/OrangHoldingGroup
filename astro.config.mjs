// @ts-check
import { defineConfig } from 'astro/config';

// Note: @astrojs/sitemap will be re-enabled once the project upgrades to Astro 5+
// In the meantime, a static sitemap is provided at public/sitemap.xml

export default defineConfig({
  site: 'https://orangholding.com',
});
